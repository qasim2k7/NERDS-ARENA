{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Raylib Debug",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/game.exe",
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "miDebuggerPath": "gdb",
            "preLaunchTask": "build"
        }
    ]
}
